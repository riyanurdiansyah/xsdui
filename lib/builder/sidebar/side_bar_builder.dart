import 'package:flutter/material.dart';

class SideBarBuilder extends StatelessWidget {
  const SideBarBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
